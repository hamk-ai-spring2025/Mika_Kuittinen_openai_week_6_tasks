# Mika_Kuittinen_openai_week_6_tasks
week_6_Numpy, Pandas and Dataframe

## Assignments

Assignment #12. Multi-chat with web interface.

Generate a chat with modern web interface, which supports more than one large language model (LLM). The user can choose, which model to use.

Assignment #13. Web-based image generator.

Generate a modern web interface, which allows to generate images using your favorite AI image generator API. It should at least support prompt (and negative prompt) and aspect ratio and offer a button to download the image.
